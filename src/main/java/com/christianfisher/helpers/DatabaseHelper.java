package com.christianfisher.helpers;

import java.util.logging.Logger;

public class DatabaseHelper {
    Logger logger = Logger.getLogger(DatabaseHelper.class.getName());
    final String CLASSNAME = DatabaseHelper.class.getSimpleName();

    /* TODO:
        - check if db exists
        - if not, create, open and set path variable
        - if yes, open and set path variable
     */

}
